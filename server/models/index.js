'use strict'

const { Article } = require('./article.model')
const { Member } = require('./member.model')
const { Topic } = require('./topic.model')

module.exports = {
  Article,
  Member,
  Topic,
}
